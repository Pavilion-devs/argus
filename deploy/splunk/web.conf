# Argus drives Splunk entirely through the management port (8089) + the MCP
# server. Splunk Web (8000) is unused and costs ~400 MB RAM, so keep it off on
# the 4 GB box. Re-enable by provisioning with SPLUNK_WEB=1 (then restart Splunk).

[settings]
startwebserver = 0
